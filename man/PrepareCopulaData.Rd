% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{PrepareCopulaData}
\alias{PrepareCopulaData}
\title{Prepare data and strata for copula modeling (Step 0)}
\usage{
PrepareCopulaData(
  data,
  idCols = character(0),
  strataCols = character(0),
  nodeCols = NULL,
  excludeCols = character(0),
  strataSpecs = list(all = list()),
  outDir = NULL
)
}
\arguments{
\item{data}{Clean input data frame.}

\item{idCols}{Character vector of identifier columns (excluded from model).}

\item{strataCols}{Character vector of exogenous stratification columns (excluded from model).}

\item{nodeCols}{Character vector of endogenous node columns. If \code{NULL}, all numeric
columns not in \code{idCols}, \code{strataCols}, or \code{excludeCols} are used.}

\item{excludeCols}{Additional columns to exclude from the model.}

\item{strataSpecs}{Named list of stratum recipes passed to \code{\link[=BuildStrata]{BuildStrata()}}. Each
recipe may include \code{mutate}, \code{filter}, \code{group_by}, \code{stratumCol}, \code{nameSep}, \code{minN}.
Keys become prefixes: stratum names are \code{"<spec_key>::<stratum_label>"}.}

\item{outDir}{Optional directory to save \code{prep.rds}.}
}
\value{
List with elements \code{data}, \code{idCols}, \code{strataCols}, \code{nodeCols}, \code{strata},
and \code{meta} (per-stratum row counts).
}
\description{
Validates column roles, resolves node variables, and builds named strata from
declarative recipes. User supplies a clean data frame; no imputation is performed.
}
