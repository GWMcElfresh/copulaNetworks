% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_mixed.R
\name{FitCopulaLatentMixedModel}
\alias{FitCopulaLatentMixedModel}
\title{Fit Bayesian copula latent mixed models}
\usage{
FitCopulaLatentMixedModel(
  data,
  nodeCols,
  latentFormula,
  engine = c("brms", "conjugate"),
  metaPrior = NULL,
  marginalSpec = NULL,
  nIter = 800L,
  burnIn = 200L,
  thin = 2L,
  chains = 2L,
  iter = 1000L,
  warmup = 500L,
  cores = 1L,
  seed = NULL,
  silent = TRUE
)
}
\arguments{
\item{data}{Data frame with node columns and covariates / grouping variables.}

\item{nodeCols}{Character vector of variables entering the copula.}

\item{latentFormula}{RHS-only formula for the \strong{latent} mean structure,
e.g. \code{~ time + treatment + (1 + time | subject)}. Does not select which
variables enter the copula (\code{nodeCols} does).}

\item{engine}{\code{"brms"} (recommended HMC) or \code{"conjugate"} (Gibbs; always
available). Conjugate emits a warning recommending brms.}

\item{metaPrior}{Optional output of \code{\link[=FitMetaCorPrior]{FitMetaCorPrior()}} for residual NIW update.}

\item{marginalSpec}{Optional \code{\link[=FitMarginalSpec]{FitMarginalSpec()}} result; if \code{NULL} and
\code{metaPrior} is supplied, uses \code{metaPrior$marginalSpec}; else same-cohort NPN.}

\item{nIter, burnIn, thin}{Conjugate MCMC controls.}

\item{chains, iter, warmup, cores}{brms MCMC controls (ignored for conjugate).}

\item{seed}{Optional RNG seed.}

\item{silent}{If \code{TRUE}, suppress brms progress output.}
}
\value{
List with camelCase fields: \code{nodeCols}, \code{latentFormula}, \code{engine},
\code{zMatrix}, \code{residualMatrix}, \code{nodeFits}, \code{fixedEffects}, \code{randomEffects},
\code{randomCov}, \code{design}, \code{marginalSpec}, and optionally \code{bayesFit} /
\code{impliedCor} when \code{metaPrior} is supplied. Also \code{residualGraphical} when
a glasso fit on residuals succeeds.
}
\description{
Random effects are introduced \strong{only} in the latent Gaussian layer after
nonparanormal (or prior-ECDF) transformation of \code{nodeCols}. A shared
RHS-only \code{latentFormula} specifies the mean structure applied to each node's
latent scores \(z_j\):
}
\details{
\deqn{z_j = X\beta_j + Z b_j + \varepsilon_j, \quad
b_j \sim \mathrm{MVN}(0, D_j), \quad
\varepsilon \sim \mathrm{MVN}(0, \Sigma)}

Cross-node dependence is captured by residual \(\Sigma\). When \code{metaPrior}
from \code{\link[=FitMetaCorPrior]{FitMetaCorPrior()}} is supplied, residual scores are passed to the
existing NIW / powerprior update (\code{\link[=FitBayesianMetaUpdate]{FitBayesianMetaUpdate()}}) without changing
the conjugate NIW machinery.

Supported random-effect structures (single grouping factor): \code{(1 | g)},
\code{(0 + x | g)}, \code{(1 + x | g)} / \code{(x | g)}.
}
\examples{
set.seed(1)
n_subj <- 20
n_time <- 4
dat <- data.frame(
  subject = rep(seq_len(n_subj), each = n_time),
  time = rep(seq_len(n_time), times = n_subj)
)
b0 <- rnorm(n_subj, 0, 0.5)
dat$y1 <- b0[dat$subject] + 0.3 * dat$time + rnorm(nrow(dat))
dat$y2 <- 0.7 * dat$y1 + rnorm(nrow(dat), sd = 0.5)
dat$y3 <- 0.4 * dat$y1 + rnorm(nrow(dat), sd = 0.7)
fit <- FitCopulaLatentMixedModel(
  dat,
  nodeCols = c("y1", "y2", "y3"),
  latentFormula = ~ time + (1 | subject),
  engine = "conjugate",
  nIter = 200,
  burnIn = 50
)
dim(fit$residualMatrix)
}
