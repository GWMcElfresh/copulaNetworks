# Symmetric color breaks centered at 0, bounded by max \|value\| in mat

Symmetric color breaks centered at 0, bounded by max \|value\| in mat

## Usage

``` r
symmetric_matrix_breaks(mat, n = 101)
```
