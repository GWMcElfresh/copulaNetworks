# Build comparison scatter plot

Build comparison scatter plot

## Usage

``` r
comparison_scatter_plot(
  comparison_df,
  labelA,
  labelB,
  title,
  label_threshold = 0.05,
  max_labels = 25
)
```
