# Map ranks to uniform pseudo-observations

Map ranks to uniform pseudo-observations

## Usage

``` r
rank_to_uniform(x, n_ref = length(x))
```

## Arguments

- x:

  Numeric vector.

- n_ref:

  Reference sample size for the denominator (default `length(x)`).

## Value

Numeric vector in (0, 1).
