# Extract all pairwise values from a symmetric matrix

Extract all pairwise values from a symmetric matrix

## Usage

``` r
extract_pairwise_values(
  mat,
  vars = colnames(mat),
  edge_only = FALSE,
  adj = NULL
)
```
