# Load a checkpoint RDS file

Load a checkpoint RDS file

## Usage

``` r
LoadCheckpoint(outDir, filename = "checkpoint.rds")
```

## Arguments

- outDir:

  Directory containing the checkpoint.

- filename:

  File name (default `checkpoint.rds`).

## Value

The loaded object.
