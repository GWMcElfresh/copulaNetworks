# Save a checkpoint RDS file

Save a checkpoint RDS file

## Usage

``` r
SaveCheckpoint(object, outDir, filename = "checkpoint.rds")
```

## Arguments

- object:

  R object to save.

- outDir:

  Output directory.

- filename:

  File name (default `checkpoint.rds`).

## Value

Invisibly returns the full file path.
